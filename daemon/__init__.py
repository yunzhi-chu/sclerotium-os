"""Sclerotium OS — Daemon.

The daemon is the organism's body — it runs as a Windows service
or background process, hosts the system tray icon, and keeps the
lifeform alive even when no terminal is open.
"""
