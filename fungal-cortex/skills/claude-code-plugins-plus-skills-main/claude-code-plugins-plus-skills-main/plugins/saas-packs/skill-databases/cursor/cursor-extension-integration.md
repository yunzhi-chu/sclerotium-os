# cursor-extension-integration

## Skill Scaffold

```
cursor-extension-integration/
    SKILL.md
```

## File Descriptions

### 1. SKILL.md

**Purpose:** Integrates VS Code extensions with Cursor including compatibility assessment, installation methods, conflict resolution, and performance optimization.

**Workflow:** This skill activates when developers need to manage extensions in Cursor. It covers compatibility, installation methods, essential extensions, recommended configurations, conflict management, and performance optimization.

**Relates to:** cursor-install-auth (initial extension setup), cursor-performance-tuning (extension performance), cursor-upgrade-migration (extension migration), cursor-keybindings (extension keybindings)

## Summary

This integration skill covers comprehensive VS Code extension management in Cursor. It includes compatibility information (what works, what conflicts with AI features), installation methods (marketplace, command line, VSIX), essential extensions by category (code quality, language support, framework tools, git), recommended configurations (extensions.json, per-extension settings), managing conflicts (AI assistants, completion providers, keybindings), extension development with Cursor AI, performance optimization (reducing overhead, workspace-specific), syncing extensions across machines, and troubleshooting.
