version: 1
generated_at: "2025-01-15T10:30:00.000Z"
source:
  repo: octocat/hello-world
  pr_number: 42
  pr_url: https://github.com/octocat/hello-world/pull/42
  base_branch: main
  head_branch: feat/rate-limiting
  author: contributor
title: "feat: add rate limiting middleware"
summary: 'Feature PR by @contributor: "feat: add rate limiting middleware" — 3 files changed (+150/-10)'
intent:
  likely_goal: Add tenant-aware API rate limiting to public routes to prevent abuse and noisy-neighbor traffic spikes.
  change_type: feature
scope:
  include:
    - src/middleware/**
    - src/routes/**
  exclude:
    - Unrelated modules not touched by this PR
affected_files:
  - filename: src/middleware/rateLimit.ts
    status: added
    additions: 100
    deletions: 0
  - filename: src/routes/api.ts
    status: modified
    additions: 30
    deletions: 5
  - filename: tests/rateLimit.test.ts
    status: added
    additions: 20
    deletions: 5
constraints:
  - Preserve existing auth behavior
  - Do not rate limit admin/internal routes
  - Existing tests must continue to pass
acceptance_criteria:
  - Changes apply cleanly to main
  - All existing tests pass
  - Public routes enforce per-tenant rate limits
  - Returns 429 on limit exceeded
  - New tests cover burst traffic
verification:
  tests_required:
    - unit
    - integration
  manual_checks:
    - Verify API endpoint behavior manually
risk_flags:
  - category: security-headers
    description: Changes to middleware (src/middleware/rateLimit.ts)
    severity: medium
open_questions:
  - Should limits be configurable per plan?
generation_prompt: |
  Re-implement the following change for the octocat/hello-world repository.

  ## Goal
  Add tenant-aware API rate limiting to public routes to prevent abuse and noisy-neighbor traffic spikes.

  ## Branch
  Apply changes to: main
  Original branch: feat/rate-limiting

  ## Affected Files
    - src/middleware/rateLimit.ts (added)
    - src/routes/api.ts (modified)
    - tests/rateLimit.test.ts (added)

  ## Scope
  Focus on: src/middleware/**, src/routes/**

  ## Constraints
  - Preserve existing auth behavior
  - Do not rate limit admin/internal routes
  - Existing tests must continue to pass

  ## Reference
  Original PR: https://github.com/octocat/hello-world/pull/42
  Author: @contributor

  Implement this change following the repository's existing patterns and conventions.
  Ensure all tests pass after making the changes.
stats:
  files_changed: 3
  additions: 150
  deletions: 10
  commits: 2
