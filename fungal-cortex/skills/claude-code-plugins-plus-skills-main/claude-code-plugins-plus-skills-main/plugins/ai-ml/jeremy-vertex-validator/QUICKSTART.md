# Quickstart

```bash
pip install google-cloud-aiplatform>=1.112.0 google-cloud-monitoring google-cloud-logging
gcloud auth application-default login
gcloud config set project YOUR_PROJECT_ID
```

Then ask Claude: "Is my Vertex AI agent ready for production?"
