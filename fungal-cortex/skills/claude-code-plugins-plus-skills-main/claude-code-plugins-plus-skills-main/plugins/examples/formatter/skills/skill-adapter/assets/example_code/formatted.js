// This is an example of formatted JavaScript code.
// This file will be auto-formatted by the 'formatter' plugin after editing.

// Here's some intentionally unformatted code to demonstrate the plugin's effect:

function myUnformattedFunction( arg1 ,arg2  ) {
if(true){
return arg1+arg2;
}else{
return arg1-arg2;
}
}


// Placeholder for your actual code.  Feel free to replace this entire file.
// The plugin will format it automatically.

function anotherExampleFunction(param1, param2) {
  console.log("This is another example.");
  return param1 * param2;
}

// You can add more complex logic here.

const myVariable = {
  name: "Example",
  value: 123,
  nested: {
    property: "nested value"
  }
};

// Remember to save the file after editing to trigger the formatter!

export { myUnformattedFunction, anotherExampleFunction, myVariable };