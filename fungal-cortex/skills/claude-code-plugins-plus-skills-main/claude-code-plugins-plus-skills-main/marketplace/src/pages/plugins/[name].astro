---
import { readFileSync } from 'fs';
import { join } from 'path';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import FaqAccordion from '../../components/FaqAccordion.astro';
import DetailCta from '../../components/DetailCta.astro';
import PluginCard from '../../components/PluginCard.astro';
import skillsCatalog from '../../data/skills-catalog.json';
import readmeSections from '../../data/readme-sections.json';
import jrigData from '../../data/jrig-data.json';

export async function getStaticPaths() {
  const catalogPath = join(process.cwd(), '../.claude-plugin/marketplace.extended.json');
  const catalog = JSON.parse(readFileSync(catalogPath, 'utf-8'));

  return catalog.plugins.map((plugin: any) => ({
    params: { name: plugin.name },
    props: { plugin },
  }));
}

const { plugin: rawPlugin } = Astro.props;

// Overlay JRig behavioral-eval results from forge_proofs (joined at build
// time by `scripts/enrich-jrig-data.mjs`). Detail page renders the
// JRig-Verified badge from `plugin.jrig`; absent entries leave the badge
// hidden via the existing `plugin.jrig?.verified` optional chain.
const plugin = (jrigData as Record<string, any>)[rawPlugin.name]
  ? { ...rawPlugin, jrig: (jrigData as Record<string, any>)[rawPlugin.name] }
  : rawPlugin;

const MARKETPLACE_SLUG = 'claude-code-plugins-plus';
const installCommand = `/plugin install ${plugin.name}@${MARKETPLACE_SLUG}`;

const readme = (readmeSections as any)[plugin.name] || null;

const formatPricing = (pricing: any) => {
  if (!pricing) return 'Free';
  if (pricing.model === 'one-time') {
    return `$${pricing.price} ${pricing.currency} (one-time)`;
  }
  return `$${pricing.price} ${pricing.currency}/${pricing.model}`;
};

const formatComponents = (components: any) => {
  if (!components) return [];
  const items = [];
  if (components.skills) items.push(`${components.skills} skill${components.skills > 1 ? 's' : ''}`);
  if (components.commands) items.push(`${components.commands} command${components.commands > 1 ? 's' : ''}`);
  if (components.agents) items.push(`${components.agents} agent${components.agents > 1 ? 's' : ''}`);
  if (components.hooks) items.push(`${components.hooks} hook${components.hooks > 1 ? 's' : ''}`);
  return items;
};

const componentList = formatComponents(plugin.components);

const getBadgeInfo = (verification: any) => {
  if (!verification?.badge) return null;
  const badges: Record<string, { label: string; color: string; icon: string }> = {
    gold: { label: 'Verified Gold', color: '#D4A017', icon: '/images/badges/verified-gold.svg' },
    silver: { label: 'Verified Silver', color: '#8E9AAF', icon: '/images/badges/verified-silver.svg' },
    bronze: { label: 'Verified Bronze', color: '#CD7F32', icon: '/images/badges/verified-bronze.svg' },
  };
  return badges[verification.badge] || null;
};

const badgeInfo = getBadgeInfo(plugin.verification);

// JRig behavioral-eval verdict (populated by /skill-creator --forge → forge_proofs table → build pipeline)
// plugin.jrig shape: { verified: boolean, layers_passed: number, total_layers: 7, baseline_delta: number }
const jrigBadge = plugin.jrig?.verified
  ? { label: 'JRig-Verified', layers: `${plugin.jrig.layers_passed}/${plugin.jrig.total_layers || 7}`, color: '#22c55e' }
  : null;

// Forge-generated provenance flag (set by /skill-creator --forge in plugin.json)
const forgeGenerated = plugin.generated === true || plugin.author_type === 'forge';

const relatedPlugins = await (async () => {
  const catalogPath = join(process.cwd(), '../.claude-plugin/marketplace.extended.json');
  const catalog = JSON.parse(readFileSync(catalogPath, 'utf-8'));
  return catalog.plugins
    .filter((p: any) => p.category === plugin.category && p.name !== plugin.name)
    .slice(0, 6);
})();

// Load skills for this plugin from the skills catalog
const pluginSkills = (skillsCatalog as any).skills
  .filter((s: any) => s.parentPlugin?.name === plugin.name)
  .sort((a: any, b: any) => a.slug.localeCompare(b.slug));

const cleanName = (name: string) => name.replace(/^"|"$/g, '');
const cleanTools = (tools: string[]) =>
  tools.map((t: string) => t.replace(/^"|"$/g, '')).filter((t: string) => t.length > 0);

const isSaasPack = plugin.category === 'saas-packs';
const hasSkills = pluginSkills.length > 0;

// Stats bar items
const statsItems: Array<{ label: string; value: string }> = [];
if (plugin.components?.skills) statsItems.push({ label: 'Skills', value: String(plugin.components.skills) });
if (plugin.components?.commands) statsItems.push({ label: 'Commands', value: String(plugin.components.commands) });
if (plugin.components?.agents) statsItems.push({ label: 'Agents', value: String(plugin.components.agents) });
if (plugin.mcpTools) statsItems.push({ label: 'MCP Tools', value: String(plugin.mcpTools) });
statsItems.push({ label: 'License', value: plugin.license || 'MIT' });
statsItems.push({ label: 'Pricing', value: formatPricing(plugin.pricing) });
---

<BaseLayout title={`${plugin.name} | Claude Code Plugins`} description={plugin.description}>
  <script type="application/ld+json" slot="head" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": plugin.name,
    "description": plugin.description,
    "applicationCategory": "DeveloperApplication",
    "operatingSystem": "Cross-platform",
    "author": { "@type": "Organization", "name": (plugin.author?.name || plugin.author) || "Intent Solutions IO" },
    "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
  })} />
  <div class="plugin-page">
    <div class="container">
      <!-- 1. Breadcrumbs (3-level) -->
      <Breadcrumbs crumbs={[
        { label: plugin.category.replace(/-/g, ' ').replace(/\b\w/g, (c: string) => c.toUpperCase()), href: `/explore?category=${plugin.category}` },
        { label: plugin.name },
      ]} />

      <!-- 2. Header -->
      <div class="header">
        <h1>
          {plugin.name}
          {badgeInfo && (
            <a href="/verification" class="verification-badge" title={`${badgeInfo.label} — Score: ${plugin.verification.score}/100`}>
              <img src={badgeInfo.icon} alt={badgeInfo.label} width="28" height="28" />
            </a>
          )}
        </h1>
        {plugin.tagline && <p class="tagline">{plugin.tagline}</p>}
        <div class="meta">
          {plugin.featured && <span class="badge featured">Featured</span>}
          {badgeInfo && (
            <a href="/verification" class="badge verified" style={`border-color: ${badgeInfo.color}; color: ${badgeInfo.color};`}>
              {badgeInfo.label} &middot; {plugin.verification.score}/100
            </a>
          )}
          {jrigBadge && (
            <a href={`/plugins/${plugin.slug || plugin.name}/verification`} class="badge jrig-verified" title={`Behavioral eval: ${jrigBadge.layers} JRig layers passed across Haiku/Sonnet/Opus`}>
              {jrigBadge.label} &middot; {jrigBadge.layers} layers
            </a>
          )}
          {forgeGenerated && (
            <span class="badge forge-generated" title="Generated by /skill-creator --forge with mandatory NOI gate + validation">
              Forge-generated
            </span>
          )}
          <span class="badge category">{plugin.category}</span>
          <span class="meta-item">v{plugin.version}</span>
          {plugin.author?.name && <span class="meta-item">by {plugin.author.name}</span>}
        </div>
      </div>

      <!-- 3. Description -->
      <p class="description">{plugin.description}</p>

      <!-- 4. Stats Bar -->
      {statsItems.length > 0 && (
        <div class="stats-bar">
          {statsItems.map((stat, i) => (
            <>
              {i > 0 && <span class="stats-divider" aria-hidden="true" />}
              <div class="stat-item">
                <span class="stat-value">{stat.value}</span>
                <span class="stat-label">{stat.label}</span>
              </div>
            </>
          ))}
        </div>
      )}

      <!-- 5. Installation -->
      <div class="section">
        <h2>Installation</h2>
        <p class="section-hint">Open Claude Code and run this command:</p>
        <div class="install-box">
          <code class="install-command">{installCommand}</code>
          <button class="copy-btn" id="copy-btn">Copy</button>
        </div>
        <p class="tip">
          Use <code>--global</code> to install for all projects, or <code>--project</code> for current project only.
        </p>
      </div>

      <!-- 6. Overview / What It Does (from README) -->
      {readme?.overview && (
        <div class="section readme-section" data-expandable>
          <h2>What It Does</h2>
          <div class="readme-content" set:html={readme.overview} />
        </div>
      )}

      <!-- 7. Features (from README) -->
      {readme?.features && (
        <div class="section readme-section features-section">
          <h2>Features</h2>
          <div class="readme-content" set:html={readme.features} />
        </div>
      )}

      <!-- 8. Skills -->
      {hasSkills ? (
        <div class="section skills-section">
          <div class="skills-header">
            <h2>Skills ({pluginSkills.length})</h2>
            {pluginSkills.length > 1 && (
              <button class="expand-all-btn" id="expand-all-btn">Expand All</button>
            )}
          </div>
          <div class="gist-list">
            {pluginSkills.map((skill: any) => (
              <div class="gist-card" data-skill-slug={skill.slug}>
                <div class="gist-header">
                  <button class="gist-toggle" aria-label="Toggle skill content">
                    <svg class="toggle-icon" width="12" height="12" viewBox="0 0 12 12">
                      <path d="M4 2 L10 6 L4 10 Z" fill="currentColor"/>
                    </svg>
                  </button>
                  <svg class="file-icon" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                    <path d="M2 1.75C2 .784 2.784 0 3.75 0h6.586c.464 0 .909.184 1.237.513l2.914 2.914c.329.328.513.773.513 1.237v9.586A1.75 1.75 0 0 1 13.25 16h-9.5A1.75 1.75 0 0 1 2 14.25Zm1.75-.25a.25.25 0 0 0-.25.25v12.5c0 .138.112.25.25.25h9.5a.25.25 0 0 0 .25-.25V6h-2.75A1.75 1.75 0 0 1 9 4.25V1.5Zm6.75.062V4.25c0 .138.112.25.25.25h2.688l-.011-.013-2.914-2.914-.013-.011Z"/>
                  </svg>
                  <span class="gist-filename">{cleanName(skill.name)}</span>
                  <span class="gist-filetype">SKILL.md</span>
                  <a href={`/skills/${skill.slug}/`} class="gist-view-link">View full skill &rarr;</a>
                </div>
                <div class="gist-body">
                  <p class="gist-description">{skill.description?.split('.')[0]}.</p>
                  {cleanTools(skill.allowedTools || []).length > 0 && (
                    <div class="gist-tools">
                      {cleanTools(skill.allowedTools).map((tool: string) => (
                        <span class="tool-pill">{tool}</span>
                      ))}
                    </div>
                  )}
                </div>
                <div class="gist-content" style="display: none;">
                  <div class="gist-content-inner" set:html={
                    skill.content
                      ? skill.content.slice(0, 3000)
                      : ''
                  } />
                </div>
              </div>
            ))}
          </div>
        </div>
      ) : componentList.length > 0 && (
        <div class="section">
          <h2>Components</h2>
          <div class="components-list">
            {componentList.map((item: string) => (
              <span class="component-badge">{item}</span>
            ))}
          </div>
        </div>
      )}

      <!-- 9. Usage / How It Works (from README) -->
      {readme?.usage && (
        <div class="section readme-section">
          <h2>How It Works</h2>
          <div class="readme-content" set:html={readme.usage} />
        </div>
      )}

      <!-- 10. Use Cases (from README) -->
      {readme?.useCases && (
        <div class="section readme-section">
          <h2>Use Cases</h2>
          <div class="readme-content" set:html={readme.useCases} />
        </div>
      )}

      <!-- 11. FAQ (from README troubleshooting) -->
      {readme?.troubleshooting && readme.troubleshooting.length > 0 && (
        <div class="section">
          <h2>FAQ</h2>
          <FaqAccordion items={readme.troubleshooting} />
        </div>
      )}

      <!-- 12. Bottom CTA -->
      <DetailCta installCommand={installCommand} pluginName={plugin.name} repositoryUrl={plugin.repository} />

      <!-- 13. Related Plugins -->
      {relatedPlugins.length > 0 && (
        <div class="section">
          <h2>Related Plugins</h2>
          <div class="related-plugins">
            {relatedPlugins.map((related: any) => (
              <PluginCard
                name={related.name}
                description={related.description}
                installCommand={`/plugin install ${related.name}@claude-code-plugins-plus`}
                href={`/plugins/${related.name}`}
              />
            ))}
          </div>
        </div>
      )}

      <!-- 14. Tags Footer -->
      {plugin.keywords && plugin.keywords.length > 0 && (
        <div class="tags-footer">
          <span class="tags-label">Tags</span>
          <div class="tags-list">
            {plugin.keywords.map((keyword: string) => (
              <span class="tag">{keyword}</span>
            ))}
          </div>
        </div>
      )}
    </div>
  </div>

  <script>
    const copyBtn = document.getElementById('copy-btn');
    const installCommand = document.querySelector('.install-command');

    if (copyBtn && installCommand) {
      copyBtn.addEventListener('click', async () => {
        const text = installCommand.textContent || '';
        await navigator.clipboard.writeText(text);

        copyBtn.textContent = 'Copied!';
        copyBtn.classList.add('copied');

        if (window.trackEvent) {
          window.trackEvent('install_copied', {
            plugin_name: text.split(' ').pop() || '',
            source: 'plugin_detail'
          });
        }

        setTimeout(() => {
          copyBtn.textContent = 'Copy';
          copyBtn.classList.remove('copied');
        }, 2000);
      });
    }

    // Gist expand/collapse
    document.querySelectorAll('.gist-toggle').forEach(btn => {
      btn.addEventListener('click', () => {
        const card = btn.closest('.gist-card');
        if (!card) return;
        const content = card.querySelector('.gist-content') as HTMLElement;
        const icon = btn.querySelector('.toggle-icon') as HTMLElement;
        if (!content || !icon) return;

        const isExpanded = content.style.display !== 'none';
        content.style.display = isExpanded ? 'none' : 'block';
        icon.style.transform = isExpanded ? '' : 'rotate(90deg)';
        card.classList.toggle('expanded', !isExpanded);
      });
    });

    // Also allow clicking the gist-header (but not links) to toggle
    document.querySelectorAll('.gist-header').forEach(header => {
      header.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        if (target.closest('a')) return;
        if (target.closest('.gist-toggle')) return;
        const toggle = header.querySelector('.gist-toggle') as HTMLElement;
        if (toggle) toggle.click();
      });
    });

    // Expand All / Collapse All
    const expandAllBtn = document.getElementById('expand-all-btn');
    if (expandAllBtn) {
      let allExpanded = false;
      expandAllBtn.addEventListener('click', () => {
        allExpanded = !allExpanded;
        expandAllBtn.textContent = allExpanded ? 'Collapse All' : 'Expand All';

        document.querySelectorAll('.gist-card').forEach(card => {
          const content = card.querySelector('.gist-content') as HTMLElement;
          const icon = card.querySelector('.toggle-icon') as HTMLElement;
          if (!content || !icon) return;

          content.style.display = allExpanded ? 'block' : 'none';
          icon.style.transform = allExpanded ? 'rotate(90deg)' : '';
          card.classList.toggle('expanded', allExpanded);
        });
      });
    }

    // Show more toggle for long readme sections
    document.querySelectorAll('[data-expandable]').forEach(section => {
      const content = section.querySelector('.readme-content') as HTMLElement;
      if (!content || content.scrollHeight <= 400) return;

      content.classList.add('truncated');
      const btn = document.createElement('button');
      btn.className = 'show-more-btn';
      btn.textContent = 'Show more';
      btn.addEventListener('click', () => {
        const isCollapsed = content.classList.contains('truncated');
        content.classList.toggle('truncated', !isCollapsed);
        btn.textContent = isCollapsed ? 'Show less' : 'Show more';
      });
      content.insertAdjacentElement('afterend', btn);
    });
  </script>
</BaseLayout>

<style>
  .plugin-page {
    padding-top: 6rem;
    padding-bottom: 4rem;
    min-height: 100vh;
  }

  .container {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--brand-orange);
    text-decoration: none;
    margin-bottom: 2rem;
    font-size: 0.95rem;
    font-family: 'Inter', system-ui, sans-serif;
    transition: opacity var(--duration-fast) var(--ease-out);
  }

  .back-link:hover {
    opacity: 0.8;
  }

  .header {
    margin-bottom: 2rem;
  }

  .header h1 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text, var(--brand-dark));
    font-family: 'Inter', system-ui, sans-serif;
  }

  .meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
    font-size: 0.9rem;
    font-family: 'Inter', system-ui, sans-serif;
  }

  .meta-item {
    color: var(--text-2, var(--brand-mid-gray));
  }

  .badge {
    background: var(--surface-2, var(--brand-light-gray));
    border: 1px solid var(--border, var(--brand-mid-gray));
    padding: 0.25rem 0.75rem;
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--text, var(--brand-dark));
  }

  .verification-badge {
    display: inline-flex;
    align-items: center;
    vertical-align: middle;
    margin-left: 0.5rem;
    text-decoration: none;
    transition: transform var(--duration-fast) var(--ease-out);
  }

  .verification-badge:hover {
    transform: scale(1.15);
  }

  .badge.verified {
    background: rgba(255, 255, 255, 0.9);
    border: 1.5px solid;
    font-weight: 600;
    text-decoration: none;
    transition: all var(--duration-fast) var(--ease-out);
  }

  .badge.verified:hover {
    opacity: 0.85;
  }

  .badge.featured {
    background: var(--primary);
    border-color: var(--brand-orange);
    color: white;
  }

  .badge.category {
    background: var(--brand-green);
    border-color: var(--brand-green);
    color: white;
    text-transform: capitalize;
  }

  /* JRig behavioral-eval badge — populated when forge_proofs row exists for this plugin */
  .badge.jrig-verified {
    background: rgba(34, 197, 94, 0.08);
    border-color: rgba(34, 197, 94, 0.4);
    color: rgb(22, 163, 74);
    font-weight: 600;
    text-decoration: none;
    transition: all var(--duration-fast) var(--ease-out);
  }
  .badge.jrig-verified:hover {
    background: rgba(34, 197, 94, 0.14);
    border-color: rgba(34, 197, 94, 0.6);
  }

  /* Forge-generated provenance flag — set in plugin.json by /skill-creator --forge */
  .badge.forge-generated {
    background: rgba(99, 102, 241, 0.08);
    border-color: rgba(99, 102, 241, 0.4);
    color: rgb(79, 70, 229);
    font-weight: 600;
  }

  /* NOI tagline — outcome-speak subtitle (6–10 words) under plugin name */
  .tagline {
    font-size: 1.05rem;
    line-height: 1.5;
    margin: 0.25rem 0 1rem;
    color: var(--text-muted, var(--brand-dark-muted, #555));
    font-style: italic;
    max-width: 60ch;
  }

  .description {
    font-size: 1.15rem;
    line-height: 1.8;
    margin-bottom: 2rem;
    color: var(--text, var(--brand-dark));
  }

  /* Stats Bar */
  .stats-bar {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1rem 1.5rem;
    background: var(--surface-2, var(--card));
    border: 1px solid var(--border, var(--brand-light-gray));
    border-radius: var(--radius-lg);
    margin-bottom: 1.5rem;
    overflow-x: auto;
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.2rem;
    white-space: nowrap;
  }

  .stat-value {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text, var(--brand-dark));
  }

  .stat-label {
    font-size: 0.7rem;
    color: var(--text-3, var(--brand-mid-gray));
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .stats-divider {
    width: 1px;
    height: 2rem;
    background: var(--border, var(--brand-light-gray));
    flex-shrink: 0;
  }

  .section {
    background: var(--surface-2, var(--card));
    border: 1px solid var(--border, var(--brand-light-gray));
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    
  }

  .section h2 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--text, var(--brand-dark));
    font-family: 'Inter', system-ui, sans-serif;
  }

  .section-hint {
    margin-bottom: 1rem;
    color: var(--text-2, var(--brand-mid-gray));
    font-size: 0.95rem;
  }

  .install-box {
    background: var(--brand-dark);
    border-radius: var(--radius-md);
    padding: 1rem 1.25rem;
    font-family: 'SF Mono', Monaco, 'Cascadia Code', monospace;
    font-size: 0.9rem;
    position: relative;
    margin-bottom: 1rem;
  }

  .install-command {
    color: var(--brand-orange-light);
    user-select: all;
  }

  .copy-btn {
    position: absolute;
    top: 0.65rem;
    right: 0.75rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--radius-sm);
    padding: 0.4rem 0.8rem;
    font-size: 0.85rem;
    color: white;
    cursor: pointer;
    transition: all var(--duration-fast) var(--ease-out);
    font-family: 'Inter', system-ui, sans-serif;
  }

  .copy-btn:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: var(--brand-orange);
  }

  .copy-btn.copied {
    background: var(--brand-green);
    border-color: var(--brand-green);
  }

  .tip {
    color: var(--text-2, var(--brand-mid-gray));
    font-size: 0.9rem;
  }

  .tip code {
    background: var(--surface-3, var(--brand-light-gray));
    padding: 0.15rem 0.4rem;
    border-radius: var(--radius-sm);
    font-size: 0.85rem;
    color: var(--text, var(--brand-dark));
  }

  /* README content sections */
  .readme-section .readme-content {
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--text, var(--brand-dark));
  }

  .readme-content.truncated {
    max-height: 400px;
    overflow: hidden;
    mask-image: linear-gradient(to bottom, black 70%, transparent 100%);
    -webkit-mask-image: linear-gradient(to bottom, black 70%, transparent 100%);
  }

  .show-more-btn {
    display: block;
    margin-top: 0.5rem;
    background: none;
    border: none;
    color: var(--primary, var(--brand-orange));
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    padding: 0.25rem 0;
  }

  .show-more-btn:hover {
    text-decoration: underline;
  }

  .readme-content :global(p) {
    margin-bottom: 0.75rem;
  }

  .readme-content :global(ul),
  .readme-content :global(ol) {
    margin-bottom: 0.75rem;
    padding-left: 1.5rem;
  }

  .readme-content :global(li) {
    margin-bottom: 0.35rem;
  }

  .features-section .readme-content :global(li)::marker {
    color: var(--primary, var(--brand-orange));
  }

  .readme-content :global(code) {
    background: var(--surface-3, var(--brand-light-gray));
    padding: 0.1rem 0.35rem;
    border-radius: 3px;
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
    font-size: 0.85rem;
  }

  .readme-content :global(pre) {
    background: var(--brand-dark);
    color: var(--brand-light, #f5f5f5);
    padding: 0.75rem 1rem;
    border-radius: var(--radius-sm);
    overflow-x: auto;
    margin-bottom: 0.75rem;
    font-size: 0.85rem;
  }

  .readme-content :global(pre code) {
    background: transparent;
    padding: 0;
    color: inherit;
  }

  .readme-content :global(strong) {
    font-weight: 600;
    color: var(--text, var(--brand-dark));
  }

  .readme-content :global(a) {
    color: var(--primary, var(--brand-orange));
    text-decoration: underline;
  }

  .readme-content :global(h3),
  .readme-content :global(h4) {
    font-weight: 600;
    color: var(--text, var(--brand-dark));
    margin-top: 1rem;
    margin-bottom: 0.5rem;
  }

  .readme-content :global(h3) {
    font-size: 1.05rem;
  }

  .readme-content :global(h4) {
    font-size: 0.95rem;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1.25rem;
  }

  .info-item {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .info-label {
    font-size: 0.85rem;
    color: var(--text-2, var(--brand-mid-gray));
    font-weight: 500;
    font-family: 'Inter', system-ui, sans-serif;
  }

  .info-value {
    font-size: 1rem;
    color: var(--text, var(--brand-dark));
    text-transform: capitalize;
  }

  .components-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .component-badge {
    background: var(--primary);
    color: white;
    padding: 0.4rem 1rem;
    border-radius: var(--radius-sm);
    font-size: 0.9rem;
    font-weight: 500;
    font-family: 'Inter', system-ui, sans-serif;
  }

  /* Skill Gist Explorer */
  .skills-section {
    padding: 1.5rem;
  }

  .skills-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
  }

  .skills-header h2 {
    margin-bottom: 0;
  }

  .expand-all-btn {
    background: var(--surface-3, var(--brand-light-gray));
    border: 1px solid var(--border, var(--brand-mid-gray));
    border-radius: var(--radius-sm);
    padding: 0.35rem 0.85rem;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text-2, var(--brand-mid-gray));
    cursor: pointer;
    font-family: 'Inter', system-ui, sans-serif;
    transition: all var(--duration-fast) var(--ease-out);
  }

  .expand-all-btn:hover {
    border-color: var(--brand-orange);
    color: var(--brand-orange);
  }

  .gist-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .gist-card {
    border: 1px solid var(--border, var(--brand-light-gray));
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: border-color var(--duration-fast) var(--ease-out);
  }

  .gist-card:hover {
    border-color: var(--border-bright, var(--brand-mid-gray));
  }

  .gist-card.expanded {
    border-color: var(--brand-orange);
  }

  .gist-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 0.85rem;
    background: var(--surface-3, #f6f8fa);
    border-bottom: 1px solid var(--border, var(--brand-light-gray));
    cursor: pointer;
    user-select: none;
  }

  .gist-toggle {
    background: none;
    border: none;
    padding: 0.15rem;
    cursor: pointer;
    color: var(--text-3, var(--brand-mid-gray));
    display: flex;
    align-items: center;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .gist-toggle:hover {
    color: var(--brand-orange);
  }

  .toggle-icon {
    transition: transform var(--duration-fast) var(--ease-out);
  }

  .file-icon {
    color: var(--text-3, var(--brand-mid-gray));
    flex-shrink: 0;
  }

  .gist-filename {
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--brand-orange);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .gist-filetype {
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
    font-size: 0.7rem;
    color: var(--text-3, var(--brand-mid-gray));
    background: var(--surface-2, var(--brand-light-gray));
    padding: 0.1rem 0.4rem;
    border-radius: var(--radius-sm);
    flex-shrink: 0;
  }

  .gist-view-link {
    margin-left: auto;
    font-size: 0.78rem;
    color: var(--text-3, var(--brand-mid-gray));
    text-decoration: none;
    white-space: nowrap;
    font-family: 'Inter', system-ui, sans-serif;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .gist-view-link:hover {
    color: var(--brand-orange);
  }

  .gist-body {
    padding: 0.75rem 0.85rem;
    background: var(--surface-1, #ffffff);
  }

  .gist-description {
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--text-2, var(--brand-mid-gray));
    margin: 0 0 0.5rem 0;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
  }

  .gist-tools {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
  }

  .tool-pill {
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
    font-size: 0.7rem;
    padding: 0.15rem 0.45rem;
    border-radius: var(--radius-sm);
    background: rgba(234, 170, 0, 0.1);
    color: var(--brand-orange);
    border: 1px solid rgba(234, 170, 0, 0.2);
  }

  .gist-content {
    border-top: 1px solid var(--border, var(--brand-light-gray));
    background: var(--surface-1, #ffffff);
    max-height: 400px;
    overflow-y: auto;
  }

  .gist-content-inner {
    padding: 1rem 0.85rem;
    font-size: 0.85rem;
    line-height: 1.7;
    color: var(--text, var(--brand-dark));
  }

  .gist-content-inner :global(code) {
    background: var(--surface-3, var(--brand-light-gray));
    padding: 0.1rem 0.35rem;
    border-radius: 3px;
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
    font-size: 0.8rem;
  }

  .gist-content-inner :global(strong) {
    color: var(--text, var(--brand-dark));
    font-weight: 600;
  }

  /* Related plugins — grid layout only; card styling lives in PluginCard. */
  .related-plugins {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
  }

  @media (min-width: 768px) {
    .related-plugins {
      grid-template-columns: repeat(auto-fill, minmax(min(280px, 100%), 1fr));
      gap: 1rem;
    }
  }

  /* Tags footer (moved keywords) */
  .tags-footer {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.6rem;
    margin-top: 0.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border, var(--brand-light-gray));
  }

  .tags-label {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--text-3, var(--brand-mid-gray));
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-family: 'Inter', system-ui, sans-serif;
  }

  .tags-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
  }

  .tag {
    background: var(--surface-3, var(--brand-light-gray));
    padding: 0.2rem 0.55rem;
    border-radius: var(--radius-sm);
    font-size: 0.72rem;
    color: var(--text-2, var(--brand-mid-gray));
    font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
  }

  .repo-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--brand-orange);
    text-decoration: none;
    font-size: 0.95rem;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 500;
  }

  .repo-link:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .plugin-page {
      padding-top: 5rem;
    }

    .header h1 {
      font-size: 1.85rem;
    }

    .stats-bar {
      gap: 0.75rem;
      padding: 0.75rem 1rem;
    }

    .info-grid {
      grid-template-columns: 1fr 1fr;
    }

    .gist-header {
      flex-wrap: wrap;
    }

    .gist-view-link {
      margin-left: 0;
      width: 100%;
      margin-top: 0.25rem;
      padding-left: 1.85rem;
    }

    .gist-filename {
      max-width: 50vw;
    }
  }
</style>
