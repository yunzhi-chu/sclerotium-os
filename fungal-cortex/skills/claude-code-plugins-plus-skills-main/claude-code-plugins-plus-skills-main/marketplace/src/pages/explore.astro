---
import BaseLayout from '../layouts/BaseLayout.astro';
import CompareBar from '../components/CompareBar.astro';
import PluginCard from '../components/PluginCard.astro';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';

// PluginCard is imported for the side-effect of registering the global
// `.pcard` stylesheet — the cards on this page are rendered client-side from
// the unified-search-index, so we apply the class via JS-built HTML below.
void PluginCard;

// Read only the stats from the search index at build time — the full items array
// (~2.5 MB JSON) is NOT inlined into HTML. It is fetched lazily at runtime from
// /data/unified-search-index.json (copied to public/data/ during build).
const _rawStats = JSON.parse(
  readFileSync(resolve(process.cwd(), 'src/data/unified-search-index.json'), 'utf-8')
).stats;

const pageTitle = 'Explore Plugins & Skills';
const pageDescription = `Search all ${_rawStats.totalPlugins} plugins and ${_rawStats.totalSkills} skills in one place. Find exactly what you need with instant fuzzy search.`;
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <style>
    /* Explorer Page Styles */

    /* Prevent horizontal scroll on mobile */
    :global(section) {
      max-width: 100vw;
      overflow-x: hidden;
    }

    .explorer-hero {
      background: var(--surface);
      color: var(--text);
      padding: 3.5rem 3rem 2.5rem;
      text-align: center;
      position: relative;
    }

    .explorer-title {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      font-weight: 700;
      margin-bottom: 1rem;
      line-height: 1.1;
    }

    .explorer-subtitle {
      font-size: 1.2rem;
      color: var(--text-2);
      max-width: 700px;
      margin: 0 auto 2.5rem;
    }

    .hero-search-box {
      max-width: 800px;
      margin: 0 auto;
      position: relative;
    }

    .hero-search-input {
      width: 100%;
      padding: 1.25rem 1.75rem;
      font-size: 1.1rem;
      border: 1px solid var(--border);
      border-radius: 12px;
      font-family: 'Inter', system-ui, sans-serif;
      background: var(--surface-1);
      color: var(--text);
      
    }

    .hero-search-input:focus {
      outline: none;
      
    }

    .hero-search-input::placeholder {
      color: var(--brand-mid-gray);
    }

    .hero-stats {
      display: flex;
      gap: 3rem;
      justify-content: center;
      margin-top: 2rem;
      font-size: 0.9rem;
      color: var(--text-2);
    }

    .hero-stat span {
      font-weight: 700;
      font-size: 1.2rem;
      display: block;
      font-family: 'JetBrains Mono', monospace;
    }

    /* Filters Section */
    .filters-bar {
      background: var(--surface-1);
      border-bottom: 2px solid var(--border);
      padding: 1.5rem 4rem;
      position: sticky;
      top: 80px;
      z-index: 50;
    }

    .filters-container {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      gap: 1.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .type-toggle {
      display: flex;
      gap: 0.5rem;
      background: var(--surface-3);
      padding: 0.25rem;
      border-radius: 8px;
    }

    .type-btn {
      font-family: 'Inter', system-ui, sans-serif;
      padding: 0.5rem 1.25rem;
      border: none;
      background: transparent;
      color: var(--brand-mid-gray);
      font-size: 0.9rem;
      font-weight: 600;
      border-radius: 6px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .type-btn.active {
      background: var(--surface-1);
      color: var(--gold);
      
    }

    .filter-select {
      padding: 0.6rem 1rem;
      border: 2px solid var(--brand-light-gray);
      border-radius: 8px;
      font-size: 0.9rem;
      font-family: 'Inter', system-ui, sans-serif;
      background: var(--surface-2);
      color: var(--text);
      cursor: pointer;
      transition: border-color 0.2s;
    }

    .filter-select:focus {
      outline: none;
      border-color: var(--gold);
    }

    .results-info {
      margin-left: auto;
      font-size: 0.875rem;
      color: var(--brand-mid-gray);
    }

    .keyboard-hint {
      display: flex;
      align-items: center;
      gap: 0.25rem;
      font-size: 0.75rem;
      color: var(--text-3);
    }

    .keyboard-hint kbd {
      background: var(--surface-3);
      border: 1px solid var(--brand-light-gray);
      border-radius: 4px;
      padding: 0.15rem 0.4rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.7rem;
    }

    @media (max-width: 768px) {
      .keyboard-hint {
        display: none;
      }
    }

    /* Results Grid */
    .results-section {
      max-width: 1400px;
      margin: 0 auto;
      padding: 3rem 4rem;
    }

    /* Results grid — layout only; card styling lives in PluginCard.
       minmax(0, 1fr) lets columns shrink below content min-content so long
       install commands in .pcard code don't overflow the viewport. */
    .results-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
    }

    @media (min-width: 768px) {
      .results-grid {
        grid-template-columns: repeat(auto-fill, minmax(min(320px, 100%), 1fr));
        gap: 1rem;
      }
    }

    /* Keyboard navigation focus state */
    .pcard.keyboard-focused {
      outline: 2px solid var(--signal);
      outline-offset: -2px;
    }

    .author-toggle {
      margin-left: 0;
    }

    .no-results {
      text-align: center;
      padding: 4rem 2rem;
      grid-column: 1 / -1;
    }

    .no-results h3 {
      font-size: 1.5rem;
      color: var(--text);
      margin-bottom: 1rem;
    }

    .no-results p {
      color: var(--brand-mid-gray);
      margin-bottom: 1.5rem;
    }

    .no-results button {
      font-family: 'Inter', system-ui, sans-serif;
      padding: 0.75rem 1.5rem;
      background: var(--gold);
      color: var(--surface);
      border: none;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
    }

    .no-results button:hover {
      background: var(--brand-orange-dark);
      transform: translateY(-2px);
    }

    /* Mobile Responsive */
    @media (max-width: 768px) {
      .explorer-hero {
        padding: 2.5rem 2rem 2rem;
      }

      .explorer-hero,
      .filters-bar,
      .results-section {
        padding-left: 2rem;
        padding-right: 2rem;
      }

      .filters-bar {
        top: 70px;
      }

      .filters-container {
        flex-direction: column;
        align-items: stretch;
        gap: 0.75rem;
      }

      .type-toggle {
        justify-content: center;
        flex-wrap: wrap;
      }

      .type-btn {
        padding: 0.4rem 0.75rem;
        font-size: 0.8rem;
      }

      .filter-select {
        width: 100%;
        max-width: none;
        min-width: 0;
      }

      .results-info {
        margin-left: 0;
        text-align: center;
      }

      .hero-stats {
        flex-direction: column;
        gap: 1rem;
      }
    }

    @media (max-width: 480px) {
      .explorer-hero {
        padding: 2rem 1rem 1.5rem;
      }

      .filters-bar,
      .results-section {
        padding-left: 1rem;
        padding-right: 1rem;
      }

      .explorer-title {
        font-size: 1.5rem;
      }

      .explorer-subtitle {
        font-size: 0.85rem;
        margin-bottom: 1.5rem;
      }

      .hero-search-input {
        font-size: 0.95rem;
        padding: 0.75rem 1rem;
      }

      .hero-stats {
        gap: 0.75rem;
      }

      .hero-stat span {
        font-size: 1rem;
      }

      .no-results {
        padding: 2rem 1rem;
      }
    }
  </style>

  <!-- Hero Section with Search -->
  <section class="explorer-hero">
    <h1 class="explorer-title">Explore Everything</h1>
    <p class="explorer-subtitle">
      Search {_rawStats.totalPlugins} plugins and {_rawStats.totalSkills} skills in one unified catalog
    </p>

    <div class="hero-search-box">
      <input
        type="text"
        id="main-search"
        class="hero-search-input"
        placeholder="Try: 'kubernetes deploy' or 'validate json' or 'terraform'"
        autocomplete="off"
      />
    </div>

    <div class="hero-stats">
      <div class="hero-stat">
        <span>{_rawStats.totalPlugins}</span>
        Plugins
      </div>
      <div class="hero-stat">
        <span>{_rawStats.totalSkills}</span>
        Skills
      </div>
      <div class="hero-stat">
        <span>{_rawStats.categories.length}</span>
        Categories
      </div>
    </div>
  </section>

  <!-- Filters Bar -->
  <div class="filters-bar">
    <div class="filters-container">
      <div class="type-toggle">
        <button class="type-btn active" data-type="all">All</button>
        <button class="type-btn" data-type="plugin">Plugins</button>
        <button class="type-btn" data-type="skill">Skills</button>
        <button class="type-btn" data-type="favorites" id="favorites-btn">♡ Favorites</button>
      </div>

      <div class="type-toggle author-toggle">
        <button class="type-btn active" data-author="all">All Authors</button>
        <button class="type-btn" data-author="official">Official</button>
        <button class="type-btn" data-author="community">Community</button>
      </div>

      <select id="category-filter" class="filter-select">
        <option value="">All Categories</option>
        {_rawStats.categories.map(category => (
          <option value={category}>
            {category.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase())}
          </option>
        ))}
      </select>

      <select id="keyword-filter" class="filter-select">
        <option value="">All Keywords</option>
        {(_rawStats.allKeywords || []).map(kw => (
          <option value={kw}>{kw.replace(/-/g, ' ')}</option>
        ))}
      </select>

      <select id="tool-filter" class="filter-select">
        <option value="">All Tools</option>
        {_rawStats.skillTools.map(tool => (
          <option value={tool}>{tool}</option>
        ))}
      </select>

      <select id="sort-filter" class="filter-select">
        <option value="relevance">Sort: Relevance</option>
        <option value="a-z">Sort: A → Z</option>
        <option value="z-a">Sort: Z → A</option>
        <option value="category">Sort: Category</option>
      </select>

      <span id="results-info" class="results-info">
        Showing {_rawStats.totalItems} items
      </span>

      <span class="keyboard-hint" title="Keyboard shortcuts: / search, j/k navigate, Enter open">
        <kbd>/</kbd> <kbd>j</kbd><kbd>k</kbd> <kbd>↵</kbd>
      </span>
    </div>
  </div>

  <!-- Results Grid — populated by client-side JS after lazy fetch of /data/unified-search-index.json -->
  <div class="results-section">
    <div id="results-grid" class="results-grid" aria-live="polite" aria-busy="true">
      <!-- Loading skeleton shown until search index fetches -->
      <div id="explore-loading" style="grid-column: 1 / -1; text-align: center; padding: 4rem 2rem; color: var(--text-2);">
        <div style="font-size: 1.1rem; margin-bottom: 0.5rem;">Loading catalog&hellip;</div>
        <div style="font-size: 0.85rem; color: var(--text-3);">Fetching search index</div>
      </div>
    </div>
  </div>

  <!-- Search & Filter Logic -->
  <script type="module">
    import Fuse from 'https://cdn.jsdelivr.net/npm/fuse.js@7.1.0/+esm';

    const grid = document.getElementById('results-grid');
    const resultsInfo = document.getElementById('results-info');
    const loadingEl = document.getElementById('explore-loading');

    // Search index is fetched lazily — not inlined into HTML.
    // Fetch begins immediately on page load but cards render once data arrives.
    let allItems = [];
    let fuse = null;
    let dataReady = false;

    const indexPromise = fetch('/data/unified-search-index.json')
      .then(r => r.json())
      .then(data => {
        allItems = data.items;
        fuse = new Fuse(allItems, {
          keys: ['name', 'description', 'category', 'keywords', 'searchText'],
          threshold: 0.3,
          includeScore: true
        });
        dataReady = true;
        // Remove loading indicator
        if (loadingEl) loadingEl.remove();
        grid.setAttribute('aria-busy', 'false');
        // Render initial view (respecting any pre-set URL params)
        applyFilters();
      })
      .catch(err => {
        console.error('Failed to load search index:', err);
        if (loadingEl) loadingEl.innerHTML = '<div style="color: var(--brand-mid-gray)">Failed to load catalog. Please refresh.</div>';
      });

    let activeFilters = {
      type: 'all',
      authorType: 'all',
      category: null,
      keyword: null,
      tool: null,
      search: '',
      sort: 'relevance'
    };

    function applyFilters() {
      // Don't attempt to render before the fetch completes
      if (!dataReady) return;

      let filtered = allItems;

      // Apply search
      if (activeFilters.search && fuse) {
        const results = fuse.search(activeFilters.search);
        filtered = results.map(r => r.item);
      }

      // Apply type filter
      if (activeFilters.type === 'favorites') {
        // Filter to only bookmarked items
        const favorites = window.BookmarkManager ? window.BookmarkManager.getAll() : [];
        filtered = filtered.filter(item => favorites.includes(item.id || item.name));
      } else if (activeFilters.type !== 'all') {
        filtered = filtered.filter(item => item.type === activeFilters.type);
      }

      // Apply author type filter
      if (activeFilters.authorType !== 'all') {
        filtered = filtered.filter(item => (item.authorType || 'official') === activeFilters.authorType);
      }

      // Apply category filter
      if (activeFilters.category) {
        filtered = filtered.filter(item => item.category === activeFilters.category);
      }

      // Apply keyword filter (plugins only)
      if (activeFilters.keyword) {
        filtered = filtered.filter(item =>
          item.keywords && item.keywords.includes(activeFilters.keyword)
        );
      }

      // Apply tool filter (skills only)
      if (activeFilters.tool) {
        filtered = filtered.filter(item =>
          item.type === 'skill' && item.allowedTools && item.allowedTools.includes(activeFilters.tool)
        );
      }

      // Apply sorting
      if (activeFilters.sort !== 'relevance' || !activeFilters.search) {
        filtered = [...filtered].sort((a, b) => {
          const nameA = (a.displayName || a.name).toLowerCase();
          const nameB = (b.displayName || b.name).toLowerCase();

          switch (activeFilters.sort) {
            case 'a-z':
              return nameA.localeCompare(nameB);
            case 'z-a':
              return nameB.localeCompare(nameA);
            case 'category':
              const catCompare = a.category.localeCompare(b.category);
              return catCompare !== 0 ? catCompare : nameA.localeCompare(nameB);
            default:
              return 0; // relevance - keep Fuse.js order
          }
        });
      }

      renderResults(filtered);
      updateResultsInfo(filtered.length);
      updateURL();
    }

    function updateURL() {
      const params = new URLSearchParams();
      if (activeFilters.search) params.set('q', activeFilters.search);
      if (activeFilters.type !== 'all') params.set('type', activeFilters.type);
      if (activeFilters.category) params.set('category', activeFilters.category);
      if (activeFilters.keyword) params.set('keyword', activeFilters.keyword);
      if (activeFilters.sort !== 'relevance') params.set('sort', activeFilters.sort);
      const newURL = params.toString()
        ? `${window.location.pathname}?${params.toString()}`
        : window.location.pathname;
      window.history.replaceState({}, '', newURL);
    }

    function escHtml(str) {
      if (!str) return '';
      return String(str).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
    }

    function renderCard(item) {
      const id = escHtml(item.id || item.name);
      const displayName = escHtml(item.type === 'plugin' && item.displayName ? item.displayName : item.name);
      const href = item.type === 'plugin'
        ? `/plugins/${escHtml(item.name)}/`
        : (item.parentPlugin ? `/cowork` : `/skills/${escHtml(item.slug)}/`);
      const installName = item.type === 'plugin' ? item.name : (item.parentPlugin?.name || item.name);
      const installCmd = `/plugin install ${escHtml(installName)}@claude-code-plugins-plus`;

      const metaParts = [];
      if (item.type === 'skill' && item.parentPlugin) {
        metaParts.push(`<span>Provided by ${escHtml(item.parentPlugin.name)}</span>`);
      }
      if (item.skillCount > 0) {
        metaParts.push(`<span>${item.skillCount} skills</span>`);
      }
      const metaHtml = metaParts.length
        ? `<small>${metaParts.join(' · ')}</small>`
        : '';

      return `<div
          data-type="${escHtml(item.type)}"
          data-category="${escHtml(item.category)}"
          data-item-id="${id}"
          data-item-name="${displayName}"
          data-author-type="${escHtml(item.authorType || 'official')}">
          <a class="pcard" href="${href}">
            <h3>${displayName}</h3>
            <p>${escHtml(item.description)}</p>
            <code>${installCmd}</code>
            ${metaHtml}
          </a>
        </div>`;
    }

    function attachCardListeners(_container) {
      // Cards are now plain links — bookmark/compare toggles moved off the
      // cards (filter rail / detail page own them).
    }

    function renderResults(items) {
      if (items.length === 0) {
        grid.innerHTML = `
          <div class="no-results">
            <h3>No results found</h3>
            <p>Try adjusting your search or filters</p>
            <button onclick="window.clearAllFilters()">Clear All Filters</button>
          </div>
        `;
        return;
      }

      // Render cards client-side (data not server-rendered to avoid 25 MB HTML payload)
      grid.innerHTML = items.map(item => renderCard(item)).join('');
      attachCardListeners(grid);
      if (window.CompareManager) window.CompareManager.updateCardStates();
    }

    function updateResultsInfo(count) {
      resultsInfo.textContent = `Showing ${count} of ${allItems.length} items`;
    }

    // Event listeners
    document.addEventListener('DOMContentLoaded', () => {
      // Read URL params on page load (from homepage redirect)
      const urlParams = new URLSearchParams(window.location.search);
      const typeParam = urlParams.get('type');
      const searchParam = urlParams.get('q');

      if (typeParam && ['all', 'plugin', 'skill'].includes(typeParam)) {
        activeFilters.type = typeParam;
        document.querySelectorAll('.type-btn').forEach(b => b.classList.remove('active'));
        const targetBtn = document.querySelector(`.type-btn[data-type="${typeParam}"]`);
        if (targetBtn) targetBtn.classList.add('active');
      }

      if (searchParam) {
        activeFilters.search = searchParam;
        document.getElementById('main-search').value = searchParam;
      }

      // Apply filters if any params were set
      if (typeParam || searchParam) {
        applyFilters();
      }

      // Search input
      let searchDebounceTimer = null;
      document.getElementById('main-search').addEventListener('input', (e) => {
        activeFilters.search = e.target.value;
        applyFilters();

        // Debounced search tracking (1s)
        clearTimeout(searchDebounceTimer);
        if (e.target.value.length >= 2) {
          searchDebounceTimer = setTimeout(() => {
            if (window.trackEvent) {
              window.trackEvent('search', { query: e.target.value, source: 'explore' });
            }
          }, 1000);
        }
      });

      // Type toggle buttons (only in the first toggle group, not author toggle)
      document.querySelectorAll('.type-toggle:not(.author-toggle) .type-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          document.querySelectorAll('.type-toggle:not(.author-toggle) .type-btn').forEach(b => b.classList.remove('active'));
          e.target.classList.add('active');
          activeFilters.type = e.target.dataset.type;
          applyFilters();
          if (window.trackEvent) {
            window.trackEvent('filter_applied', { filter_type: 'type', value: activeFilters.type });
          }
        });
      });

      // Author type toggle buttons
      document.querySelectorAll('.author-toggle .type-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          document.querySelectorAll('.author-toggle .type-btn').forEach(b => b.classList.remove('active'));
          e.target.classList.add('active');
          activeFilters.authorType = e.target.dataset.author;
          applyFilters();
          if (window.trackEvent) {
            window.trackEvent('filter_applied', { filter_type: 'author', value: activeFilters.authorType });
          }
        });
      });

      // Category filter
      document.getElementById('category-filter').addEventListener('change', (e) => {
        activeFilters.category = e.target.value || null;
        applyFilters();
        if (window.trackEvent && activeFilters.category) {
          window.trackEvent('filter_applied', { filter_type: 'category', value: activeFilters.category });
        }
      });

      // Keyword filter
      document.getElementById('keyword-filter').addEventListener('change', (e) => {
        activeFilters.keyword = e.target.value || null;
        applyFilters();
        if (window.trackEvent && activeFilters.keyword) {
          window.trackEvent('filter_applied', { filter_type: 'keyword', value: activeFilters.keyword });
        }
      });

      // Tool filter
      document.getElementById('tool-filter').addEventListener('change', (e) => {
        activeFilters.tool = e.target.value || null;
        applyFilters();
        if (window.trackEvent && activeFilters.tool) {
          window.trackEvent('filter_applied', { filter_type: 'tool', value: activeFilters.tool });
        }
      });

      // Sort filter
      document.getElementById('sort-filter').addEventListener('change', (e) => {
        activeFilters.sort = e.target.value;
        applyFilters();
        if (window.trackEvent) {
          window.trackEvent('filter_applied', { filter_type: 'sort', value: activeFilters.sort });
        }
      });

      // Read additional URL params (category, sort)
      const categoryParam = urlParams.get('category');
      const sortParam = urlParams.get('sort');

      if (categoryParam) {
        activeFilters.category = categoryParam;
        document.getElementById('category-filter').value = categoryParam;
      }

      const keywordParam = urlParams.get('keyword');
      if (keywordParam) {
        activeFilters.keyword = keywordParam;
        document.getElementById('keyword-filter').value = keywordParam;
      }

      if (sortParam && ['relevance', 'a-z', 'z-a', 'category'].includes(sortParam)) {
        activeFilters.sort = sortParam;
        document.getElementById('sort-filter').value = sortParam;
      }

      // Keyboard shortcuts
      let focusedCardIndex = -1;

      function updateCardFocus(newIndex) {
        const cards = document.querySelectorAll('.pcard');
        if (cards.length === 0) return;

        if (focusedCardIndex >= 0 && focusedCardIndex < cards.length) {
          cards[focusedCardIndex].classList.remove('keyboard-focused');
        }

        focusedCardIndex = Math.max(0, Math.min(newIndex, cards.length - 1));

        const focusedCard = cards[focusedCardIndex];
        focusedCard.classList.add('keyboard-focused');
        focusedCard.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }

      document.addEventListener('keydown', (e) => {
        const inInput = ['INPUT', 'TEXTAREA'].includes(document.activeElement.tagName);

        if (e.key === '/' && !inInput) {
          e.preventDefault();
          document.getElementById('main-search').focus();
        }

        if (e.key === 'Escape') {
          if (document.activeElement.id === 'main-search') {
            document.getElementById('main-search').blur();
            if (activeFilters.search) {
              activeFilters.search = '';
              document.getElementById('main-search').value = '';
              applyFilters();
            }
          }
          const focusedCard = document.querySelector('.pcard.keyboard-focused');
          if (focusedCard) {
            focusedCard.classList.remove('keyboard-focused');
            focusedCardIndex = -1;
          }
        }

        if (!inInput) {
          if (e.key === 'j' || e.key === 'ArrowDown') {
            e.preventDefault();
            updateCardFocus(focusedCardIndex + 1);
          }
          if (e.key === 'k' || e.key === 'ArrowUp') {
            e.preventDefault();
            updateCardFocus(focusedCardIndex - 1);
          }
          if (e.key === 'Enter') {
            const focusedCard = document.querySelector('.pcard.keyboard-focused');
            if (focusedCard) {
              e.preventDefault();
              window.location.href = focusedCard.href;
            }
          }
        }
      });

      // Clear all filters function
      window.clearAllFilters = () => {
        activeFilters = { type: 'all', category: null, keyword: null, tool: null, search: '', sort: 'relevance' };
        document.getElementById('main-search').value = '';
        document.getElementById('category-filter').value = '';
        document.getElementById('keyword-filter').value = '';
        document.getElementById('tool-filter').value = '';
        document.getElementById('sort-filter').value = 'relevance';
        document.querySelectorAll('.type-btn').forEach(b => b.classList.remove('active'));
        document.querySelector('.type-btn[data-type="all"]').classList.add('active');
        applyFilters();
      };

      // Listen for reapply-filters event (from BookmarkManager)
      window.addEventListener('reapply-filters', () => {
        applyFilters();
      });
    });
  </script>

  <!-- Bookmark/Favorites functionality -->
  <script is:inline>
    window.BookmarkManager = {
      favorites: new Set(),

      init() {
        const saved = localStorage.getItem('plugin-favorites');
        if (saved) {
          try {
            this.favorites = new Set(JSON.parse(saved));
          } catch (e) {
            this.favorites = new Set();
          }
        }
        this.updateUI();
        this.updateFavoritesCount();
      },

      toggle(id) {
        var action;
        if (this.favorites.has(id)) {
          this.favorites.delete(id);
          this.showToast('Removed from favorites');
          action = 'remove';
        } else {
          this.favorites.add(id);
          this.showToast('Added to favorites');
          action = 'add';
        }
        if (window.trackEvent) {
          window.trackEvent('favorite_toggled', { plugin_name: id, action: action });
        }
        this.save();
        this.updateUI();
        this.updateFavoritesCount();

        // Re-apply filters if favorites filter is active
        const favBtn = document.querySelector('.type-btn[data-type="favorites"]');
        if (favBtn && favBtn.classList.contains('active')) {
          window.dispatchEvent(new CustomEvent('reapply-filters'));
        }
      },

      has(id) {
        return this.favorites.has(id);
      },

      getAll() {
        return Array.from(this.favorites);
      },

      save() {
        localStorage.setItem('plugin-favorites', JSON.stringify(Array.from(this.favorites)));
      },

      updateUI() {
        document.querySelectorAll('.bookmark-btn').forEach(btn => {
          const id = btn.dataset.bookmarkId;
          if (this.has(id)) {
            btn.classList.add('active');
            btn.textContent = '♥';
            btn.title = 'Remove from favorites';
          } else {
            btn.classList.remove('active');
            btn.textContent = '♡';
            btn.title = 'Add to favorites';
          }
        });
      },

      updateFavoritesCount() {
        const favBtn = document.getElementById('favorites-btn');
        if (favBtn) {
          const count = this.favorites.size;
          favBtn.textContent = count > 0 ? `♥ Favorites (${count})` : '♡ Favorites';
        }
      },

      showToast(message) {
        const existing = document.querySelector('.bookmark-toast');
        if (existing) existing.remove();

        const toast = document.createElement('div');
        toast.className = 'bookmark-toast';
        toast.textContent = message;
        toast.style.cssText = `
          position: fixed;
          bottom: 2rem;
          left: 50%;
          transform: translateX(-50%);
          background: #1a1a1a;
          color: white;
          padding: 0.75rem 1.25rem;
          border-radius: 8px;
          font-size: 0.9rem;
          z-index: 1001;
          animation: fadeInOut 2s ease-in-out forwards;
        `;
        document.body.appendChild(toast);
        setTimeout(() => toast.remove(), 2000);
      }
    };

    document.addEventListener('DOMContentLoaded', () => {
      BookmarkManager.init();

      // Add click handlers to bookmark buttons
      document.querySelectorAll('.bookmark-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          BookmarkManager.toggle(btn.dataset.bookmarkId);
        });
      });
    });
  </script>

  <CompareBar />
</BaseLayout>
