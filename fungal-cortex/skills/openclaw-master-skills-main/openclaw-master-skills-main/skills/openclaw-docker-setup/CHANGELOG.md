# Changelog

## v1.0.0 — 2026-03-03

Initial release.

- Full macOS Docker install workflow (Steps 0–8)
- Step 0: Auto-detect existing instances, suggest next free name and port
- Multi-instance support: configurable `$INSTANCE` and `$HOST_PORT` throughout
- Port and name conflict detection before launch
- Optional Gmail via Himalaya (attachment support)
- Optional Google Drive/Docs/Sheets/Calendar via gog
- 14+ known pitfalls documented with fixes
- Reviewer score: 33/33
- Tester: PASS (verified against live container)
