# 方正鸿云编辑助手 - 安装与配置指南

## 📦 安装

### 方式一：Clawhub 安装（推荐）

```bash
# 在 Clawhub 中搜索 "方正鸿云编辑助手" 并安装
```

### 方式二：手动安装（ClawHub）

```bash
# 1. 访问 ClawHub 技能页面
# https://clawhub.ai/behurry/founder-hy-editor-browser

# 2. 点击 Download 按钮下载技能包

# 3. 解压到 OpenClaw 技能目录
unzip founder-hy-editor-browser.zip -d ~/.openclaw/skills/

# 4. 验证安装
ls ~/.openclaw/skills/founder-hy-editor-browser/
# 应显示：SKILL.md, _meta.json, setup.md
```

---

## 🔐 环境变量配置

以下环境变量均为**可选**，如未设置，技能会在需要时提示输入：

| 变量名 | 必需 | 默认值 | 说明 |
|--------|------|--------|------|
| `FOUNDER_PLATFORM_URL` | ❌ | `http://journal.portal.founderss.cn/` | 平台登录地址 |
| `FOUNDER_USERNAME` | ❌ | 无 | **不建议设置，推荐使用时输入** |
| `FOUNDER_PASSWORD` | ❌ | 无 | **不建议设置，推荐使用时输入** |

### 配置方式

#### 方式一：系统环境变量（仅平台地址）

```bash
export FOUNDER_PLATFORM_URL="http://journal.portal.founderss.cn/"
```

#### 方式二：使用时输入（推荐）

不设置用户名和密码环境变量，首次使用时通过"登录方正鸿云"功能手动输入用户名和密码。

**优势**：
- ✅ 凭证不落地，无持久化存储风险
- ✅ 每次登录需用户确认，安全性更高
- ✅ 符合最小权限原则

---

## 🚀 快速开始

### 1. 登录平台

```
登录方正鸿云
```

### 2. 获取待处理任务

```
鸿云任务提醒
```

### 3. 查询过期稿件

```
自动催修
自动催审
```

### 4. 注册 DOI

```
自动注册 DOI
```

---

## 📋 功能清单

| 功能 | 触发词 | 说明 |
|------|--------|------|
| 登录方正鸿云 | `登录方正鸿云` | 打开平台并完成登录 |
| 获取登录 Cookie | `获取登录 Cookie` | 提取 Cookie 存储到会话 |
| 切换刊物 | `切换刊物` | 切换当前刊物 |
| 自动催修 | `自动催修` | 查询退修中过期稿件（支持分页） |
| 自动催审 | `自动催审` | 查询送专家审过期稿件 |
| 鸿云任务提醒 | `鸿云任务提醒` | 汇总工作台待处理任务 |
| 自动填写送审单 | `自动填写送审单` | AI 选专家 + 填写送审附言 |
| 自动注册 DOI | `自动注册 DOI` | 全自动 DOI 注册（最新刊期） |
| 调用获取刊物信息接口 | `调用获取刊物信息接口` | 获取刊物 ID 和名称 |
| 调用获取发布站点接口 | `调用获取发布站点接口` | 获取站点 ID |
| 调用获取刊期列表接口 | `调用获取刊期列表接口` | 获取刊期列表 |
| 调用获取刊期论文列表接口 | `调用获取刊期论文列表接口` | 获取指定刊期的论文列表 |
| 调用 DOI 注册接口 | `调用 DOI 注册接口` | 批量提交 DOI 注册 |

---

## 🔒 安全说明

### 凭证管理

- ✅ **不硬编码凭证** - 用户名和密码通过环境变量或使用时输入
- ✅ **会话临时存储** - Cookie 存储在会话内存，结束后清除
- ⚠️ **建议** - 敏感操作后重新获取 Cookie

### 安全最佳实践

1. 不要在公开场合分享你的登录凭据
2. 定期更换平台登录密码
3. 使用完毕后会话自动清除 Cookie
4. 建议在可信环境中使用此技能

---

## 📞 技术支持

- **平台方**: 方正鸿云团队
- **平台网址**: http://journal.portal.founderss.cn/
- **ClawHub 页面**: https://clawhub.ai/behurry/founder-hy-editor-browser

---

## 📄 许可证

MIT License
