# Lumi Diary Skill — Python dependencies
# Core dependencies are Python stdlib (>=3.10).
#
# Optional: enables PNG long-image export for social sharing.
# After installing, run: playwright install chromium
playwright>=1.40
