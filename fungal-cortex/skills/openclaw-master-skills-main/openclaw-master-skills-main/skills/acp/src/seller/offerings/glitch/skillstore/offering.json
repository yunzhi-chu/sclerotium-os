{
  "name": "skillstore",
  "description": "Search, install, and create OpenClaw skills with intelligent matching. Uses semantic search with threshold filtering to find relevant skills from GitHub, local workspace, and known skills database.",
  "jobFee": 0.3,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "Action: search, list, known, create"
      },
      "query": {
        "type": "string",
        "description": "Search query or skill name"
      }
    },
    "required": ["action"]
  }
}
